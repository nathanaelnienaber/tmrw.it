# Resend configuration
RESEND_API_KEY=your_resend_api_key_here
# Optional: override the friendly from address (must be verified in Resend)
RESEND_FROM="TMRW Studio <hello@tmrw.it>"

# Optional: override who receives contact emails (comma separated list)
CONTACT_EMAIL_RECIPIENTS=hello@tmrw.it
